name: Ship
on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      bump:
        description: Version bump type
        required: true
        type: choice
        options:
          - patch
          - minor
          - major
jobs:
  register:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow registered"
